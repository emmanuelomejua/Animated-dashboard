export type chartProps = {
    color: string;
    icon: string;
    title: string;
    dataKey: number | string;
    number: number | string;
    percentage: number;
    chartData: object[];
}

export type barchartBoxProps = {
    title: string;
    color: string;
    dataKey: string;
    chartData: object[];
  }